<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.training.modules.exam.mapper.ExamScoreMapper">
    <resultMap id="SMap" type="com.company.training.modules.exam.entity.ExamScore">
        <id property="id" column="id"/><result property="examId" column="exam_id"/><result property="userId" column="user_id"/>
        <result property="answers" column="answers"/><result property="score" column="score"/><result property="submitTime" column="submit_time"/><result property="status" column="status"/><result property="createTime" column="create_time"/><result property="reviewer" column="reviewer"/><result property="reviewTime" column="review_time"/><result property="deleted" column="deleted"/>
    </resultMap>
    <insert id="insert" parameterType="com.company.training.modules.exam.entity.ExamScore" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO exam_score(exam_id,user_id,answers,score,submit_time,status,create_time,reviewer,review_time,deleted)
        VALUES(#{examId},#{userId},#{answers},#{score},NOW(),#{status},NOW(),#{reviewer},#{reviewTime},0)
    </insert>
    <update id="updateReview">UPDATE exam_score SET score=#{score},status=#{status},reviewer=#{reviewer},review_time=NOW() WHERE id=#{id} AND deleted=0</update>
    <select id="selectByExamAndUser" resultMap="SMap">SELECT * FROM exam_score WHERE exam_id=#{examId} AND user_id=#{userId} AND deleted=0 LIMIT 1</select>
    <select id="selectPage" resultMap="SMap">SELECT * FROM exam_score WHERE deleted=0 <if test="examId!=null">AND exam_id=#{examId}</if><if test="userId!=null">AND user_id=#{userId}</if> ORDER BY id DESC LIMIT #{offset()},#{pageSize}</select>
    <select id="countPage" resultType="java.lang.Long">SELECT COUNT(1) FROM exam_score WHERE deleted=0 <if test="examId!=null">AND exam_id=#{examId}</if><if test="userId!=null">AND user_id=#{userId}</if></select>
    <select id="avg" resultType="java.lang.Double">SELECT AVG(score) FROM exam_score WHERE exam_id=#{examId} AND deleted=0</select>
    <select id="max" resultType="java.lang.Integer">SELECT MAX(score) FROM exam_score WHERE exam_id=#{examId} AND deleted=0</select>
    <select id="min" resultType="java.lang.Integer">SELECT MIN(score) FROM exam_score WHERE exam_id=#{examId} AND deleted=0</select>
</mapper>
